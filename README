Kboot, a kexec booloader

Before compiling, modify in custom/kboot-minimize ABCBOX variable to point to abcbox binary

Kboot python source are in fs/kboot/opt/menu

- General configuration:
make menuconfig

- uClibc configuration:
make uclibc-menuconfig

- busybox configuration:
make busybox-menuconfig

Compilation:
make
